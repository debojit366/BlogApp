import React from 'react'

const BlogCard = ({title,description}) => {
  return (
    <div>
        <p>title is: {title}</p>
        <p>decription is: {description}</p>
    </div>
  )
}

export default BlogCard